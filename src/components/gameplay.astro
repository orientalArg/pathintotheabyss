  ---
const {gameplay, btn} = Astro.props;
const {features, intro, title} = gameplay;
---
<section class="gameplay" id="gameplay">
  <div class="content">
    <div class="_title">
      <h1 class="title">{title}</h1>
      <small>{intro}</small>
    </div>
    <ul class="list">
      {features.map((a) => <li><p>{a.p}</p></li> )}
    </ul>
  </div>
  <div class="video">
    <div class="iframe_wrapper" id="iframe_wrapper">
      <div class="play_video" id="play_video">
        <span class="play">
          <p>{btn.play}</p>
          <img src="/images/icons/play.svg" alt="">
        </span>
        <span class="pause">
          <p>{btn.pause}</p>
          <img src="/images/icons/pause.svg" alt="">
        </span>
      </div>
      <iframe id="iframe" width="560" height="315" src="https://www.youtube.com/embed/9s-5tR0fGfM?si=BW70FephbJCklA3d=1?vq=hd1080&autoplay=0" title={intro} frameborder="0" allow=" picture-in-picture; autoplay;" allowfullscreen></iframe>
    </div>
  </div>
</section>

<script>
  let togglePlay = document.querySelectorAll("#play_video")!;
  let iframe = document.getElementById("iframe")!;
  let wrapper = document.getElementById("iframe_wrapper")!;

  if(screen.width > 900) {
    window.addEventListener('load', togglePlayer);
  }

  togglePlay.forEach((btn) => btn.addEventListener("click", togglePlayer))

  function togglePlayer() {
    wrapper.classList.toggle("float")

    togglePlay.forEach((btn) => btn.classList.toggle("toggle_btn"))

    if(iframe.getAttribute("src") == "https://www.youtube.com/embed/9s-5tR0fGfM?si=BW70FephbJCklA3d=1?vq=hd1080&autoplay=0") {
      return iframe.setAttribute("src", "https://www.youtube.com/embed/9s-5tR0fGfM?si=BW70FephbJCklA3d=1?vq=hd1080&autoplay=1")
    } else {
      return iframe.setAttribute("src", "https://www.youtube.com/embed/9s-5tR0fGfM?si=BW70FephbJCklA3d=1?vq=hd1080&autoplay=0")
    }
  }
</script>

<style lang="scss">
  .float {
    right: 0;
    bottom: 2em;
    width: 100%;
    height: 100%;
    padding: 10px;
    position: fixed;
    max-width: 600px;
    max-height: 350px;
    @media (max-width: 600px) {
      max-width: 100;
      max-height: 30vh;
    }
    @media (min-width: 900px) {
      right: 2em;
      bottom: 5vh;
      max-width: 600px;
      max-height: 350px;
    }
    @media (min-width: 1440px) {
      max-width: 700px;
      max-height: 400px;
    }
  }
  .gameplay {
    gap: 10vw;
    height: 770px;
    display: flex;
    overflow: hidden;
    padding-top: 2vw;
    justify-content: space-between;
    background-position: top center;
    background-image: url(/images/tunnel.svg);
    background-size: cover;
    .content {
      gap: 10vw;
      display: flex;
      max-width: 440px;
      flex-direction: column;
      justify-content: center;
      ._title .title {
        text-transform: uppercase;
      }
      .list {
        padding: 0px;
        li {
          list-style: disc;
          color: var(--orange);
          p {
            margin: 1em 0;
            font-weight: 300;
          }
        }
      }
    }
    .video {
      z-index:3;
      width: 100%;
      height: 450px;
      margin: auto 0;
      position: relative;
      border-radius: 15px;
      &::before {
        top: 50%;
        left: 50%;
        z-index: -1;
        content: "";
        position: absolute;
        border-radius: 15px;
        width: calc(100% + 7vw);
        height: calc(100% + 7vw);
        background: var(--black);
        transform: translate(-50%,-50%);
      }
      .iframe_wrapper {
        height: 100%;
        .play_video {
          bottom: 10px;
          right: 10px;
          z-index: 20;
          position: absolute;
          padding: 1em;
          color: grey;
          cursor: pointer;
          font-weight: 600;
          text-transform: uppercase;
          background-color: var(--black);
          gap: 1em;
          display: flex;
          align-items: center;
          span { gap:1em; align-items: center }
          .play {display: flex;}
          .pause {display: none;}
          &:hover {background-color: var(--violet);}
        }
        .toggle_btn {
          .play {display: none;}
          .pause {display: flex;}
        }
        iframe {
          width :100%;
          height:100%;
          position: relative;
          border-radius: 15px;
        }
      }
    }
  }
  @media (max-width: 1000px) {
    .gameplay {
      height: auto;
      flex-direction: column;
      .content {
        max-width: 80%;
        margin: 0 auto;
        text-align: center;
        h1 {margin: 0 auto;}
        .list li {list-style: none;}
      }
      .video {
        width: 100%;
        margin: 0 auto;
        max-width: 750px;
        .iframe_wrapper .play_video span p {
          display: none;
        }
      }
    }
  }
</style>